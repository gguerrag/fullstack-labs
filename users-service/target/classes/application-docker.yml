server:
  port: 8080   # dentro del contenedor

spring:
  application:
    name: users-service

  datasource:
    url: jdbc:oracle:thin:@x4jr6t1j9re665hr_high?TNS_ADMIN=/app/wallet
    username: LAB_USERS
    password: P4rM3d-Users#2025!
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect

springdoc:
  swagger-ui:
    path: /swagger-ui

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
